# Security Scanning with Trivy
# Scans repository filesystem and Docker images for vulnerabilities
# Fails on HIGH or CRITICAL severity findings
name: Security Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Minimal permissions following least-privilege principle
permissions:
  contents: read
  security-events: write

# Prevent concurrent scans on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  filesystem-scan:
    name: Filesystem Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      # Checkout repository code
      - name: Checkout code
        uses: actions/checkout@v4

      # Cache Trivy vulnerability database for faster subsequent runs
      - name: Cache Trivy DB
        uses: actions/cache@v4
        with:
          path: ~/.cache/trivy
          key: trivy-db-${{ github.run_id }}
          restore-keys: |
            trivy-db-

      # Scan repository filesystem for vulnerabilities in dependencies and configs
      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '1'
          severity: 'HIGH,CRITICAL'
          skip-dirs: 'node_modules,.git'
          scanners: 'vuln,secret,misconfig'
          trivyignores: '.trivyignore'
        env:
          TRIVY_SKIP_DIRS: 'node_modules,.git'

      # Generate detailed report for artifacts
      - name: Generate filesystem scan report
        if: always()
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-fs-results.sarif'
          severity: 'HIGH,CRITICAL'
          skip-dirs: 'node_modules,.git'
          scanners: 'vuln,secret,misconfig'
        env:
          TRIVY_SKIP_DIRS: 'node_modules,.git'

      # Upload SARIF report to GitHub Security tab
      - name: Upload filesystem scan results
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: 'trivy-fs-results.sarif'
          category: 'trivy-fs'

  scan-summary:
    name: Security Scan Summary
    runs-on: ubuntu-latest
    needs: [filesystem-scan]
    if: always()

    steps:
      # Create summary of all security scans
      - name: Generate scan summary
        run: |
          echo "# 🔒 Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All security scans completed. Check individual job results for details." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Scans performed:**" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Filesystem scan (dependencies, secrets, misconfigurations)" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Backend Docker image scan" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ Frontend Docker image scan" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "View detailed results in the Security tab." >> $GITHUB_STEP_SUMMARY
