# Railway configuration for landing service
[build]
builder = "dockerfile"
dockerfilePath = "packages/landing/Dockerfile"

[build.buildArgs]
PUBLIC_APP_URL = https://${{UI.RAILWAY_PUBLIC_DOMAIN}}

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 2
